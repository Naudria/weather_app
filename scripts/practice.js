// const state = document.querySelector('h6.my-3')

// if(cityDets.Country === 'United States'){
	// 	let stateSrc = document.createTextNode(`${cityDets.AdministrativeArea.EnglishName}`)
	// } else {
	// 	let stateSrc = document.createTextNode(`${cityDets.Country.EnglishName}`)
	// };
	// state.appendChild(stateSrc)
